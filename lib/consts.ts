// ── Application Constants — Ministry of Transport Platform ────────────────────
// Single source of truth for all shared constant values.

/** ₦10,000 registration fee for Mass Transit companies (stored as kobo) */
export const TRANSIT_REGISTRATION_FEE_KOBO = 10_000_00;

/** Default pagination page size for staff list views */
export const PAGE_SIZE = 20;

/** Admin/audit pagination page size */
export const ADMIN_PAGE_SIZE = 25;
