/**
 * The applicant's copy of their revalidation certificate. See the sibling
 * certificate route for why this path exists.
 */
export { default } from "@/app/(dashboard)/admin/revalidation-queue/[id]/park-certificate/page";
